# 🚀 星河智安项目启动指南

## 📋 系统要求

**通用要求：**
- Python 3.8+ (推荐3.11)
- Node.js 16+ (推荐18)
- 现代浏览器 (Chrome、Firefox、Safari、Edge)

---

## 💻 Windows系统详细启动步骤

### 🔧 后端启动

#### 步骤1: 进入后端目录
```powershell
cd backend
```

#### 步骤2: 激活虚拟环境
```powershell
.\venv\Scripts\activate
```
> **注意**: 如果虚拟环境不存在，先创建：
> ```powershell
> python -m venv venv
> .\venv\Scripts\activate
> ```

#### 步骤3: 安装核心依赖
```powershell
pip install -r requirements.txt
```

#### 步骤4: 安装额外依赖 (解决兼容性问题)
```powershell
pip install email-validator python-multipart torch torchvision pillow
```

#### 步骤5: 安装可视化依赖 (跳过版本冲突)
```powershell
pip install matplotlib seaborn plotly scikit-learn tqdm opencv-python ultralytics
```

#### 步骤6: 启动后端服务
```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

> **预期输出**:
> ```
> INFO:     Started reloader process [xxxx]
> INFO:     ✅ 星河智安平台启动完成!
> INFO:     Application startup complete.
> ```

---

### 🎨 前端启动

#### 步骤1: 新开PowerShell窗口，进入前端目录
```powershell
cd web
```

#### 步骤2: 安装依赖 (使用legacy-peer-deps)
```powershell
npm install --legacy-peer-deps
```

#### 步骤3: 配置环境变量
```powershell
echo SKIP_PREFLIGHT_CHECK=true > .env
echo REACT_APP_API_BASE_URL=http://localhost:8000 >> .env
```

#### 步骤4: 启动前端服务
```powershell
npm start
```

> **预期输出**:
> ```
> Compiled successfully!
> You can now view xinghe-zhi-an-frontend in the browser.
> http://localhost:3000
> webpack compiled successfully
> ```

---

## 🌐 访问地址

启动成功后，可以通过以下地址访问：

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs
- **C&W攻击页面**: http://localhost:3000/attacks/cw

## 🔑 默认账号

- 用户名：`admin`
- 密码：`admin123`

---

## ⚠️ 常见问题与解决方案

### 1. numpy编译错误

**问题**: `ERROR: Failed to build 'numpy' when getting requirements to build wheel`

**解决方案**:
```powershell
# 跳过有问题的旧版本，使用已安装的新版本
pip install matplotlib seaborn plotly scikit-learn tqdm opencv-python ultralytics
```

### 2. pydantic-core编译错误

**问题**: `Cannot import 'maturin'` 或 Rust编译错误

**解决方案**:
```powershell
# 使用预编译版本
pip install pydantic pydantic-settings python-dotenv httpx
```

### 3. 端口占用问题

**问题**: `Something is already running on port 3000`

**解决方案**:
```powershell
# 终止所有node进程
taskkill /F /IM node.exe

# 重新启动前端
npm start
```

### 4. 依赖版本冲突

**问题**: 依赖包版本不兼容导致的编译失败

**解决方案**:
```powershell
# 使用legacy-peer-deps安装
npm install --legacy-peer-deps

# 或跳过严格版本检查
pip install package_name --upgrade
```

---

## 🎯 快速验证步骤

### 1. 验证后端
```powershell
curl http://localhost:8000/health
```

### 2. 验证前端
- 打开浏览器访问 http://localhost:3000
- 使用 admin/admin123 登录
- 检查页面是否正常加载

### 3. 验证API文档
- 访问 http://localhost:8000/docs
- 检查Swagger UI是否正常显示

---

## 📊 启动成功标志

✅ **后端启动成功标志**:
```
INFO:     ✅ 星河智安平台启动完成!
INFO:     ⚔️ 已注册攻击算法: 1 个
INFO:    - 分类模型: 1 个
INFO:    - 检测模型: 0 个
```

✅ **前端启动成功标志**:
```
Compiled successfully!
You can now view xinghe-zhi-an-frontend in the browser.
webpack compiled successfully
```

---

## 🛠️ 开发调试

### 后端调试命令
```powershell
# 查看详细日志
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --log-level debug

# 测试特定API
curl http://localhost:8000/api/v1/attacks/cw/params/schema
```

### 前端调试
- 打开浏览器开发者工具 (F12)
- 查看Console和Network标签
- 检查API请求响应状态

---

## 📝 开发注意事项

1. **虚拟环境**: 始终在激活的虚拟环境中工作
2. **端口管理**: 确保8000和3000端口未被占用
3. **依赖版本**: 遇到编译问题时优先使用预编译版本
4. **日志监控**: 启动后监控日志输出确认服务状态
5. **浏览器缓存**: 前端更新后可能需要清除浏览器缓存

---

<div align="center">

**让AI安全研究更简单、更直观**

Made with ❤️ by 星河智安团队

</div>
