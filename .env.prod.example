POSTGRES_DB=xinghe_lab
POSTGRES_USER=xinghe
POSTGRES_PASSWORD=change-this-database-password

DEBUG=false
SECRET_KEY=change-this-to-a-long-random-string
JWT_SECRET_KEY=change-this-to-another-long-random-string

IMAGE_TAG=latest
WEB_PORT=8080
BACKEND_PORT=8000

# If web and API are served from the same host through the included Nginx,
# keep VITE_API_BASE_URL empty when building the web image.
CORS_ORIGINS=["http://localhost:8080"]

ALLOWED_IMAGE_TYPES=["jpg","jpeg","png","bmp","tiff"]
CELERY_CONCURRENCY=1
CELERY_LOG_LEVEL=info
